yes, but use redis cache for chatbot.
 {
        "status": "started",
        "user_id": state.user_id,
        "pending_fields": state.pending_fields,
        "next_question": question,
    }
and i want the chatbot in single api. user id verification should be there. and according to the name greetings should be given to the user and ask the questions to the user. 